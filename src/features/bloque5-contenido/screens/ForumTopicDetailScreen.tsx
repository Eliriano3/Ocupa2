/**
 * Tema — Bloque 5 · Contenido, foro y perfil.
 * Endpoints: GET /forum/topics/{id}, POST /forum/topics/{id}/comments
 *
 * TODO(Bloque 5 · Contenido, foro y perfil): pantalla pendiente de implementar.
 * Detalle del tema con sus comentarios y el campo para comentar.
 *
 * Los tipos y las funciones del API ya están listos en `src/api`.
 * Cuando la implementes, borra `<PlaceholderScreen />` y escribe la pantalla real.
 */

import { PlaceholderScreen } from '@/components';

export default function ForumTopicDetailScreen() {
  return (
    <PlaceholderScreen
      block="Bloque 5 · Contenido, foro y perfil"
      owner="Katerin Cordero Cubilete · 2024-1575"
      screen="Tema"
      endpoints={['GET /forum/topics/{id}', 'POST /forum/topics/{id}/comments']}
      todo="Detalle del tema con sus comentarios y el campo para comentar."
    />
  );
}
