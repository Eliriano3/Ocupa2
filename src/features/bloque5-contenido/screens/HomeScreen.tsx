/**
 * Inicio — Bloque 5 · Contenido, foro y perfil.
 *
 * TODO(Bloque 5 · Contenido, foro y perfil): pantalla pendiente de implementar.
 * Pantalla de bienvenida con un slider de imágenes y mensajes, y accesos a las demás secciones.
 *
 * Los tipos y las funciones del API ya están listos en `src/api`.
 * Cuando la implementes, borra `<PlaceholderScreen />` y escribe la pantalla real.
 */

import { PlaceholderScreen } from '@/components';

export default function HomeScreen() {
  return (
    <PlaceholderScreen
      block="Bloque 5 · Contenido, foro y perfil"
      owner="Katerin Cordero Cubilete · 2024-1575"
      screen="Inicio"
      todo="Pantalla de bienvenida con un slider de imágenes y mensajes, y accesos a las demás secciones."
      notes={[
        "Es la primera pantalla que ve el usuario, con o sin sesión iniciada.",
      ]}
    />
  );
}
