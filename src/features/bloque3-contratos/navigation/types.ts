/**
 * Rutas del Bloque 3 · Aplicantes y contratos.
 * Agrega aquí las pantallas nuevas de tu bloque: este archivo es solo tuyo.
 */

export type ContractsStackParamList = {
  MyContracts: undefined;
  ContractDetail: { contractId: string };
  ApplicantsList: { offerId: string };
  ApplicantDetail: { applicationId: string };
};
