/**
 * Me gusta — Bloque 4 · Explorar, aplicar y me gusta.
 * Endpoints: GET /me/likes, DELETE /offers/{id}/like
 *
 * TODO(Bloque 4 · Explorar, aplicar y me gusta): pantalla pendiente de implementar.
 * Listado de las ofertas a las que di me gusta, con la opción de quitarlo.
 *
 * Los tipos y las funciones del API ya están listos en `src/api`.
 * Cuando la implementes, borra `<PlaceholderScreen />` y escribe la pantalla real.
 */

import { PlaceholderScreen } from '@/components';

export default function MyLikesScreen() {
  return (
    <PlaceholderScreen
      block="Bloque 4 · Explorar, aplicar y me gusta"
      owner="Rosmeris Jimenez De La Cruz · 2024-1779"
      screen="Me gusta"
      endpoints={['GET /me/likes', 'DELETE /offers/{id}/like']}
      todo="Listado de las ofertas a las que di me gusta, con la opción de quitarlo."
    />
  );
}
